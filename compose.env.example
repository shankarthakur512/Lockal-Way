# Docker Compose deployment settings
LOKALWAY_HTTP_PORT=80

# Override these only when using custom host-side paths for env files.
SERVER_ENV_FILE=./server/.env
CLIENT_ENV_FILE=./client/.env

# Optional image override hooks for future registry-based deployments.
SERVER_IMAGE=lokalway-server:local
CLIENT_IMAGE=lokalway-client:local
